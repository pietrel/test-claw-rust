{
  "type": "function",
  "function": {
    "name": "Glob",
    "description": "Find files based on a pattern (e.g.: *.rs, src/**/*.txt)",
    "parameters": {
      "type": "object",
      "required": ["pattern"],
      "properties": {
        "pattern": {
          "type": "string",
          "description": "Glob pattern describing file paths to find"
        }
      }
    }
  }
}
